apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: {{ .Release.Name }}-regcred
  namespace: {{ .Release.Namespace }}
data:
  .dockerconfigjson: |
    {{- .Values.secretDocker | b64enc | nindent 4 }}